# Legacy: Recall Rover (abandoned concept)

The `recall_rover/`, `apps/`, `tests/`, `docs/dimos-notes.md`, `docs/architecture.md`, and the old
`scripts/demo.sh` / `Makefile` targets belong to the previous project (spatial-memory robot with
YOLO + OpenAI). They are left in place so nothing referencing them breaks, but **SignalHound does
not import any of it**. SignalHound lives in `signalhound/`, `firmware/`, `scripts/*.py` and
`docs/pivot-status.md`, `docs/hackathon-status.md`. Delete the legacy tree after the hackathon.
